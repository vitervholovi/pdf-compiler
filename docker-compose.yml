services:
  pdf-compiler:
    build: .
    ports:
      - "3080:3080"
    environment:
      - PUBLIC_BASE=/pdf-compiler
    volumes:
      - pdf_data:/app/server/data
    restart: unless-stopped

volumes:
  pdf_data:
